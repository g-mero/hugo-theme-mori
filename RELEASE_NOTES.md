## What's New

- Lazy load images, will guess image size if not provided, provided different placeholder when loading or on error.
- Added `getMoriConfig()` and `setMoriConfig()` to windows for easier access to Mori configuration in custom scripts.
- Added dark-light switcher
- Added configuration `postsSection` to set which section contains posts.
